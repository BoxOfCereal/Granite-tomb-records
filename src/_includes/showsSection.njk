<section class=" bg-black">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl uppercase mb-12 text-center">Shows</h2>
        <div
            class="grid sm:grid-cols-2 md:grid-cols-3 gap-4">
            <!-- Photo n -->
            {% for img in collections.posterImages %}
                <div class="bg-black  border border-light overflow-hidden">
                    <img src="{{ img.src }}" alt="{{ img.alt }}" loading="lazy" class="w-full h-full object-cover"></div>
                {% endfor %}
            </div>
        </div>
    </section>