{# bandCard.njk - Band Card Partial #}
<a href="/bands/{{ band.file | replace('.md','') }}/" class="block hover:scale-105 transition-transform">
  <div class="band-card bg-dark p-6 border border-light text-center">
    {% if band.image %}
      <img src="{{ band.image }}" alt="Photo of {{ band.name }}" class="w-32 h-32 mx-auto mb-4 object-cover rounded-full border-2 border-light bg-black" loading="lazy" />
    {% else %}
      <div class="w-32 h-32 mx-auto mb-4 bg-black"></div>
    {% endif %}
    <h3 class="text-lg uppercase mb-2">{{ band.name }}</h3>
    <p class="text-muted text-sm">{{ band.genre }} / {{ band.country }}</p>
  </div>
</a>
